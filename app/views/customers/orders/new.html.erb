<div class="container">
<div class="row">
  <div class="col-xs-12">
    <h2>注文情報入力</h2>
  

    
    <div>
      <%= form_with :model => @order, url:customers_orders_path, method: :post, local: true  do |f| %>
        <h3><i class="fas fa-yen-sign"></i>支払い方法</h3>
          <div>
            <%= f.radio_button :method_of_payment, :"クレジットカード", checked: true %>
            <span>クレジットカード</span>
          </div>
          <div>
            <%= f.radio_button :method_of_payment, :"銀行振込" %>
            <span>銀行振り込み</span>
          </div>

        <h3><i class="fas fa-people-carry"></i>お届け先</h3>
          <div>
            <%= f.radio_button :addresses, "residence", checked: true  %>
            <span>ご自身の住所</span><br>
            <%= "#{current_customer.postal_code} #{current_customer.address}" %><br>
            <%= "#{current_customer.first_name} #{current_customer.last_name}" %>
          </div>

          <div>
            <%= f.radio_button :addresses, "new_address" %>
            
            <span>新しいお届け先</span><br>
            <div>
              <%= f.label :"郵便番号(ハイフンなし)" %>
              <%= f.text_field :postal_code %>
            </div>
            <div>
              <%= f.label :"住所" %>
              <%= f.text_field :address %>
            </div>
            <div>
              <%= f.label :"宛名" %>
              <%= f.text_field :name %>
            </div>
          </div>

          <div>
            <%= link_to customers_cart_items_path do %>
             カートへ戻る
            <% end %>
            <%= f.submit "確認画面へ進む", class: "btn btn-success" %>
          </div>
      <% end %>
    </div>
if文でカート内に何も
      <!-- if カートアイテムがない時 -->
      <p>お客様のショッピングカートに商品はありませんので注文情報の入力ができません。</p>
      <p><%= link_to "商品一覧", customers_products_path, class: "btn btn-warning" %>から商品をお探し下さい。</p>

  </div>
</div>
</div>